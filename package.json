{
  "private": true,
  "type": "module",
  "description": "Marks js/ as ES modules so the Node-based tools in tools/ can import the game's pure-math modules directly. The site itself has no build step."
}
